# Copy to .env and fill in your values
# .env is git-ignored — NEVER commit real secrets
JWT_SECRET=dev-secret-change-in-prod-minimum-32-characters-long
POSTGRES_PASSWORD=devpassword123
GRAFANA_PASSWORD=admin